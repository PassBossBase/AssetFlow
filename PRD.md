# AssetFlow PRD

## 1. 产品概述

AssetFlow 是一个面向开发者、设计师和游戏开发者的数字资产工作空间。用户可在项目中集中管理图片、动图、矢量素材与三维模型，并快速完成预览、检索和下载。

产品目标是减少素材分散、命名混乱和预览成本，让用户能够在一个清晰、安全的工作空间中复用已有资源。

## 2. 目标用户

### 前端开发者

需要集中管理 UI 图片、SVG 图标、演示素材和三维资源，并能快速定位可复用文件。

### 设计师

需要整理插画、动图和设计资源，保留清晰的文件名称、类型和项目信息。

### 游戏开发者

需要在浏览器中预览角色、场景和道具等三维资源，减少在多个工具间切换。

## 3. 核心用户流程

```text
登录或注册
→ 进入工作台
→ 创建或选择项目
→ 上传素材
→ 搜索、筛选或预览素材
→ 下载、重命名或删除素材
```

## 4. 当前产品能力

### 4.1 账户与个人资料

- 用户可通过邮箱和密码注册、登录、退出登录或切换账号。
- 个人中心支持头像、显示名称和界面语言设置；登录邮箱仅展示，不在个人中心修改。
- 侧边栏与账户菜单同步显示用户头像和显示名称。
- 支持 English / 简体中文界面切换。

### 4.2 工作台

工作台展示用户当前工作内容的摘要：

- 项目总数
- 素材总数
- 素材存储占用
- 最近素材
- 最近项目

### 4.3 项目管理

项目包含名称、描述、创建时间和素材数量。

- 用户可创建、编辑和删除项目。
- 项目名称为 2 至 80 个字符，项目描述最多 500 个字符。
- 项目列表始终提供“新建项目”卡片。
- 用户可通过拖动手柄互换两个项目卡片的位置；拖动时显示卡片预览，目标卡片以虚线边框提示。
- 删除项目会清理其关联素材、上传任务和存储文件。

### 4.4 素材管理

支持以下类型：PNG、JPG、WEBP、SVG、GIF、GLB、GLTF；单个文件最大 250 MB。

素材卡片与详情页展示名称、原始文件名、类型、大小和时间等信息，并支持：

- 上传进度、失败重试和任务删除
- 按名称搜索
- 按类型筛选
- 重命名
- 下载
- 删除

### 4.5 素材预览

- 图片、SVG、GIF：在线等比预览。
- GLB / GLTF：浏览器内三维预览，支持拖动旋转与滚轮缩放。
- 素材详情页将预览、元数据和操作整合在同一页面。
- 对加载、预览失败和不存在的素材提供明确状态。

## 5. 数据与访问边界

- 项目、素材、上传任务和个人资料均归属于当前登录用户。
- 用户仅能访问和操作自己的项目与素材。
- 删除项目或素材时，需要同步删除对应存储文件，避免遗留文件。
- 文件操作前必须进行用户和项目归属校验。

## 6. 已知限制

- 独立 GLTF 如果依赖外部 `.bin` 或纹理文件，无法保证完整预览。
- 图片以等比方式展示，暂不提供独立缩放控制。
- 素材名称和项目名称需遵守长度限制，过长内容在界面中应可换行或截断展示。

## 7. 视觉与交互方向

产品采用深色专业工作台界面：

- 深色背景与克制的玻璃面板建立内容层级。
- 青色高光用于选中状态、主要操作和重点内容，不替代语义颜色。
- 素材预览优先于装饰效果；元数据与操作按钮保持清晰可读。
- Hover、拖动与表单操作提供轻量反馈，并支持减少动态效果设置。
- 文本、按钮和表单控件满足 WCAG AA 对比度要求。

## 8. 多语言与内容规范

- 品牌名称、项目名称、文件名和用户输入内容不自动翻译。
- 系统界面、错误状态、加载状态和操作反馈提供 English / 简体中文文案。
- 文件类型、大小、日期等元数据保持可扫描和一致的格式。
