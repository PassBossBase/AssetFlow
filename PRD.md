# AssetFlow AI PRD

## 1. 产品概述

产品名称：

AssetFlow AI

产品定位：

一个面向开发者、设计师、游戏开发者的 AI 数字资产管理平台。

核心目标：

帮助用户管理、查看、搜索和复用各种数字素材。

支持素材：

- 图片
- GIF
- SVG
- GLB
- GLTF

---

# 2. 用户画像

## 用户1：前端开发者

场景：

开发 Web 项目时，需要管理：

- UI图片
- SVG图标
- Demo素材
- 3D资源

痛点：

素材分散，查找困难。

---

## 用户2：设计师

场景：

管理：

- 插画
- 动画
- 设计资源

痛点：

文件命名混乱，版本难管理。

---

## 用户3：游戏开发者

场景：

管理：

- 角色模型
- 场景模型
- 动画资源

痛点：

无法快速预览3D模型。

---

# 3. 产品核心流程

用户流程：

进入网站

↓

创建项目

↓

上传素材

↓

系统保存素材信息

↓

查看素材

↓

下载或复用素材

---

# 4. 核心功能

## 4.1 项目空间

用户可以创建多个项目。

项目包含：

- 项目名称
- 项目描述
- 创建时间
- 素材数量

项目管理页面默认提供“新建项目”卡片；用户可以通过拖动手柄，将任意两个项目卡片的位置互换。拖动时应显示完整卡片预览，并用目标卡片的白色虚线边框明确提示放置位置。

示例：

    游戏项目

    网站设计

    AI实验

---

## 4.2 素材管理

支持上传：

- png
- jpg
- webp
- svg
- gif
- glb
- gltf

素材展示：

卡片形式。

卡片包含：

- 缩略图
- 文件名称
- 类型
- 大小
- 创建时间

---

## 4.3 素材预览

图片：

支持：

- 查看
- 放大

GIF：

支持：

- 在线播放

SVG：

支持：

- 在线展示

GLB / GLTF：

支持：

- 浏览器3D查看
- 旋转
- 缩放

---

## 4.4 标签系统

素材支持标签。

例如：

    角色
    游戏
    卡通
    UI

支持：

- 标签筛选
- 标签搜索

---

## 4.5 AI智能整理

上传素材后：

AI生成：

- 素材名称
- 标签
- 描述

示例：

输入：

    IMG001.png

输出：

    名称：
    卡通角色


    标签：
    人物
    卡通
    游戏

---

## 4.6 工作台与个人中心

工作台用于帮助用户快速回到最近工作内容，包含：

- 项目、素材和素材存储占用统计
- 最近素材
- 最近项目

个人中心用于维护账户资料与界面偏好，包含：

- 个人头像
- 显示名称
- 登录邮箱（只读）
- 中英文界面语言

账户菜单提供个人中心、切换账号和退出登录入口。切换账号会退出当前会话并回到登录页。

---

# 5. MVP范围

第一版本只实现：

必须：

- 用户登录
- 创建项目
- 上传素材
- 素材列表
- 图片预览
- GIF预览
- 文件下载

暂不实现：

- 团队协作
- 权限系统
- 版本管理

---

# 6. 后续规划

## V2

增加：

- 标签系统
- AI自动命名、标签和描述
- AI搜索

## V3

增加：

- 团队空间
- 成员权限
- 评论

## V4

增加：

- 素材版本管理
- 使用记录
- 相似素材推荐

---

# 7. 当前实施状态（2026-07-30）

当前项目已完成 Phase 1 至 Phase 3，处于 **Phase 4 素材预览收尾验收**。近期完成了工作台、项目管理和个人中心的体验完善，但未进入 Phase 5。

已实现：

- 邮箱密码认证、注册、登录、退出登录、切换账号；浏览器不保存明文密码。
- 工作台展示项目数、素材数、存储占用、最近素材和最近项目。
- 项目创建、编辑、删除、素材数量展示与卡片位置互换；删除项目会级联清理素材、上传任务和 Convex File Storage 文件。
- PNG、JPG、WEBP、SVG、GIF、GLB、GLTF 上传；素材搜索、类型筛选、重命名、删除、下载、上传成功自动收起、失败重试和任务删除。
- 图片、SVG、GIF 在线预览；GLB / GLTF 的浏览器内预览、拖动旋转和滚轮缩放。
- 素材详情、加载 / 不存在 / 预览失败状态；详情页支持直接编辑素材名称。
- 个人中心的头像、显示名称、登录邮箱展示和语言偏好；账户菜单信息同步更新。

当前差异与收尾项：

- 图片预览采用等比展示，尚未提供独立的放大 / 缩放控制。
- 独立 GLTF 如依赖外部 `.bin` 或纹理文件，仍无法保证完整预览。
- 密码找回的邮件验证与重设流程尚未接入邮件服务。
- 标签系统与 AI 整理能力尚未实现，属于 Phase 5，不应作为当前验收项。

---

# 8. 产品成功标准

用户可以：

1. 创建项目

2. 上传素材

3. 快速找到素材

4. 查看不同类型素材

5. 在 Phase 5 完成后，使用 AI 自动整理素材

---

# 9. 视觉设计方向

## 9.1 整体定位

产品视觉定位为：

    Dark Holographic Workspace

以深色专业工作台为基础，结合玻璃拟态和全息幻彩光效，形成具有未来感但仍然清晰、可信赖的数字资产管理体验。

核心感受：

- 清透
- 专业
- 未来感
- 流动感
- 高价值内容优先

全息效果用于强化品牌识别和内容层级，不用于让所有界面元素都变成彩虹渐变。

## 9.2 视觉原则

### 背景

- 使用深夜蓝、炭黑、蓝灰作为主要背景
- 使用青色、品红、紫色和珍珠白构成局部全息光场
- 光场保持缓慢、柔和、低频流动
- 避免高频闪烁和大面积过饱和渐变

### 玻璃材质

- 使用半透明面板、背景模糊和柔和内侧高光
- 卡片通过透明度、模糊和边缘描边建立层级
- 主要卡片可以使用虹彩渐变描边或局部棱镜反射
- 普通内容区域保持低强度玻璃，避免视觉噪声

### 色彩

- 主要文字使用高对比的白色或浅灰色
- 次要文字使用冷灰色和透明度层级
- 全息色彩主要用于品牌高光、选中状态、CTA 和重点素材
- 错误、成功、警告等语义状态使用稳定的语义颜色，不使用彩虹渐变

### 字体

- 标题使用清晰的几何无衬线字体
- 正文保持高可读性
- 文件类型、尺寸、时间等元信息可以使用等宽字体
- 长文本和关键操作不使用渐变文字

## 9.3 页面差异化

### Landing Page

- 使用较强的全息光效表达品牌和产品价值
- Hero 采用左侧文案、右侧产品视觉的非对称布局
- CTA 可以使用渐变金属或棱镜高光
- 首屏保持清晰层级，标题、说明和 CTA 必须完整可见

### Dashboard

- 使用克制版玻璃态，优先保证搜索、筛选、项目和素材信息的可扫描性
- Sidebar 和 Header 可以使用浮动玻璃容器
- 仅对当前项目、精选素材和关键状态使用较明显的全息高光
- 不为每张素材卡片添加持续动画或强烈彩色外发光

### Project 和 Asset 页面

- 以素材内容和文件信息为视觉中心
- 预览区域可以使用全息边缘光和柔和反射
- 元数据、标签和操作区域保持稳定、清晰、可读
- Hover 只提供轻微反射、阴影和边缘高光反馈

## 9.4 交互和动效

- Hover 反馈时间建议为 150-250ms
- Active 状态允许轻微下沉或缩放至 0.98
- 背景光场可以使用 15-20 秒的缓慢流动动画
- shimmer、倾斜和色相变化只用于重点组件
- 动效必须支持减少动态效果设置
- 移动端降低模糊强度和动画数量

## 9.5 可访问性和边界

- 文字和按钮必须满足 WCAG AA 对比度要求
- 全息渐变不能作为长文本的唯一承载背景
- 浏览器不支持背景模糊时，必须提供稳定的实色背景回退
- 不使用装饰效果代替语义提示
- 不使用 Three.js 制作普通页面背景，Three.js 仅用于 GLB / GLTF 预览

---

# 10. 多语言支持

## 10.1 支持范围

产品基础界面支持：

- English
- 简体中文

多语言能力优先覆盖用户完成核心任务所需的界面：

- 首页
- 用户登录和注册
- Dashboard
- 项目创建、编辑和删除
- 项目详情
- 错误、加载、空状态和操作反馈

品牌名称、文件名、项目名称和用户输入内容不进行自动翻译。

## 10.2 切换方式

- 在全局界面提供中英文切换控件
- 当前语言使用本地浏览器存储保存
- 用户刷新页面或切换路由后保持语言选择
- 首次访问默认使用 English
- 切换语言后同步更新页面语言标识，便于浏览器和辅助技术识别

## 10.3 文案规范

- 中文使用简体中文，避免中英文混杂
- 英文保持简洁、清晰和产品化表达
- 错误、成功、警告和加载状态必须提供对应语言文案
- 动态项目名称、素材名称、邮箱和文件元数据保持用户原始输入
- 长文本不使用全息渐变作为唯一承载方式

## 10.4 技术边界

当前阶段采用前端轻量语言映射和本地持久化方案，不引入服务端国际化路由和翻译管理平台。

后续如果支持团队空间、邀请邮件、AI生成内容或更多地区语言，再评估：

- 服务端语言检测
- URL 语言路由
- 翻译资源拆分
- 日期、数字和文件大小的本地化格式

---

# 11. Phase 3 素材管理实施范围

## 11.1 本阶段目标

在项目空间内建立可用的素材管理基础能力：

- 上传素材文件
- 保存素材元数据
- 查看项目内素材列表
- 搜索素材名称
- 按文件类型筛选
- 重命名素材
- 删除素材及其存储文件

## 11.2 支持类型和限制

当前支持：

- png
- jpg / jpeg
- webp
- svg
- gif
- glb
- gltf

单个文件最大 250 MB。素材必须归属于一个项目，并且用户只能访问自己项目中的素材。

## 11.3 存储和数据模型

Phase 3 使用 Convex File Storage 完成开发环境中的文件上传和存储，素材表保存：

- 项目 ID
- 用户 ID（Convex Auth 的稳定用户 ID，不使用会话 ID）
- Storage ID
- 素材名称
- 原始文件名
- 扩展名
- MIME 类型
- 文件大小
- 创建时间
- 更新时间

文件访问权限由 Convex 函数在上传、查询、重命名和删除操作前校验。历史会话 ID 格式的资源归属会在用户首次认证后迁移为稳定用户 ID。后续可将存储适配层迁移到 Cloudflare R2。

## 11.4 本阶段边界

Phase 3 暂不实现：

- 图片和 GIF 在线预览
- SVG 放大预览
- GLB / GLTF 三维预览
- 文件下载体验优化
- 标签系统
- AI 自动命名、标签和描述

以上能力分别留给 Phase 4 和 Phase 5。

---

# 12. Phase 4 素材预览实施范围

## 12.1 本阶段目标

让用户可以在项目内快速识别素材，并进入独立详情页查看和操作单个文件：

- 图片、SVG、GIF 在线预览
- GLB / GLTF 浏览器内三维预览
- 素材元数据查看
- 原始文件下载
- 素材重命名和删除
- 预览加载、失败和不存在状态

## 12.2 预览交互

- 项目素材卡片显示预览缩略图或类型回退
- 点击缩略图或“打开预览”进入 `/asset/[id]`
- 图片类素材使用等比缩放，避免裁切核心内容
- 3D 素材支持拖动旋转和滚轮缩放，不添加无意义的持续动画
- 预览区域使用克制的玻璃态容器和局部全息光场

## 12.3 三维预览边界

- Three.js / React Three Fiber 仅用于 GLB / GLTF 预览
- GLB 文件优先保证完整预览体验
- 依赖外部 `.bin` 或纹理文件的 GLTF 可能无法独立预览，需要后续资产关联能力支持
- 移动端降低渲染分辨率和预览区域高度

## 12.4 本阶段不实现

- 素材标签系统
- AI 自动命名、标签和描述
- 多文件关联和版本管理
- 团队协作与评论
