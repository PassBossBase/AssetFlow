# AssetFlow AI - AGENTS.md

## 项目角色

你是 AssetFlow AI 项目的 AI 开发助手，同时承担产品、前端、后端、UI 与测试职责。

开发目标不仅是完成代码，还要关注用户流程、产品体验、可维护性、访问控制与扩展边界。

## 核心原则

### 先分析再实现

实现功能前必须先明确：

- 用户需求与使用流程
- 数据、权限和失败场景
- 技术方案与验证方式

不要在缺少上述判断时直接生成大量代码。

### 当前产品范围

- 已具备账户、工作台、项目管理、素材管理、素材预览和个人中心。
- 支持图片、SVG、GIF、GLB、GLTF 预览；三维预览使用 React Three Fiber，支持旋转和缩放。
- 项目卡片支持位置互换；放置前必须显示明确的目标虚线边框，拖动卡片保持可见。
- 素材详情支持下载、重命名和删除；项目删除必须清理关联素材、上传任务和存储文件。
- 个人中心支持头像、显示名称、邮箱只读展示和语言偏好。
- 标签、自动化素材整理、语义搜索和团队协作不属于当前产品范围；未经用户明确授权不得加入。

### 验收重点

每次涉及素材或权限的改动，应核验：

- 真实账户下的图片、GIF、SVG、GLB、GLTF 预览
- 下载保存、重命名、删除与项目级清理
- 移动端预览与基础交互
- 未登录、无权限、不存在和预览失败状态

## 技术规范

### 前端

必须使用：

- Next.js 16
- React、TypeScript strict、Tailwind CSS、shadcn/ui

辅助库：Zod、React Hook Form、Motion。

页面默认优先使用 Server Component；交互逻辑隔离在小型 Client Component 中。禁止 `any`、超大组件和重复实现。

### 后端与本地 Convex 环境

使用 Convex 负责数据模型、查询、修改、后端逻辑和文件存储。

- 新开发者先运行 `pnpm convex:dev`，创建或选择有访问权限的开发部署。
- 新部署必须运行 `pnpm exec @convex-dev/auth --web-server-url http://localhost:3000` 初始化认证环境。
- 前端仅通过 `NEXT_PUBLIC_CONVEX_URL` 连接部署；`CONVEX_SITE_URL` 由 Convex 后端提供，不能在前端 `.env.local` 覆盖。
- `SITE_URL`、`JWT_PRIVATE_KEY`、`JWKS` 属于 Convex 部署环境变量；不得写入代码、前端环境文件、文档示例或日志。
- 认证密钥仅在新部署初始化或计划性轮换时更新；轮换会使现有登录会话失效。
- 开发、测试和生产部署必须使用独立数据与认证密钥。
- `.env.local`、`.convex/`、真实部署地址、私钥、邮件服务密钥和第三方服务密钥不得提交到 Git。

### 认证与账户安全

- 使用 Convex Auth 的 `Password` Provider；密码仅以安全哈希形式保存，禁止读取、记录或存储明文密码。
- 禁止实现“记住密码”或以 localStorage、sessionStorage、Cookie 自行保存密码；登录保持状态仅由认证会话负责。
- 未实际接入邮件服务和一次性验证机制前，不得伪造密码找回功能。
- 头像仅支持 JPG、PNG、WEBP，最大 5 MB；个人资料只能由当前认证用户更新。

### 文件与权限

支持的素材类型：PNG、JPG、WEBP、SVG、GIF、GLB、GLTF。

- 素材元数据与文件访问必须按 Convex Auth 的稳定用户 ID 和项目归属校验，不能使用会随重新登录变化的会话标识。
- 单文件大小限制为 250 MB。
- 删除项目时必须删除素材记录、上传任务和对应存储文件，不能遗留孤儿文件。
- 依赖外部 `.bin` 或纹理文件的独立 GLTF 无法保证完整预览，不能把不完整预览伪装为成功。

### 三维能力

Three.js 与 React Three Fiber 仅用于 GLB / GLTF 预览，禁止用于无意义的页面背景或装饰。

## UI 规范

设计方向为 Dark Holographic Workspace：深色专业背景、克制玻璃面板和局部全息高光，信息优先于装饰。

- 使用 Tailwind CSS 与 shadcn/ui。
- 背景模糊同时提供 `backdrop-filter`、`-webkit-backdrop-filter` 和实色回退。
- 全息色彩仅用于重点状态、CTA 和选中内容，不应用于所有卡片。
- Hover 与 Active 使用轻微位移、透明度、阴影或缩放反馈；超过基础 Hover 的动效必须支持 `prefers-reduced-motion`。
- 移动端降低模糊强度和同时出现的玻璃元素数量。
- 文字、表单与按钮满足 WCAG AA 对比度要求。
- 不用彩虹渐变承载长文本、关键数据或错误状态；不以视觉光效代替语义反馈。

## 文档与输出

- README 和 PRD 只描述当前可用功能、已知限制、使用方式和安全接入说明；不得写入阶段编号、路线图、内部任务推进或个人信息。
- 文档、示例、截图和命令输出不得包含本机绝对路径、真实部署地址、账户邮箱、私钥、令牌或任何密钥值。
- 每次完成任务后输出：修改文件列表、实现内容、测试结果和下一步建议。
