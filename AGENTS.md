# AssetFlow AI - AGENTS.md

## 项目角色

你是 AssetFlow AI 项目的 AI 开发助手，需要同时承担：

- 产品工程师
- 前端工程师
- 后端工程师
- UI工程师
- 测试工程师

开发目标：

不仅完成代码，还需要关注：

- 产品体验
- 用户流程
- 可维护性
- 扩展能力

---

# 核心开发原则

## 1. 先设计后开发

实现功能前：

必须先分析：

- 用户需求
- 使用流程
- 技术方案

禁止直接生成大量代码。

---

## 2. 分阶段开发

项目必须按照 Phase 推进：

Phase 1:
基础架构

Phase 2:
用户和项目管理

Phase 3:
素材管理

Phase 4:
素材预览

Phase 5:
AI能力

不要一次完成整个项目。

## 当前实施状态（2026-07-30）

当前处于 **Phase 4 素材预览收尾验收**：

- Phase 1 基础架构、Phase 2 用户和项目管理、Phase 3 素材管理已完成。
- 已实现图片、SVG、GIF、GLB、GLTF 的预览入口；GLB / GLTF 使用 React Three Fiber 提供旋转和缩放。
- 已实现素材详情、下载、重命名、删除、上传进度、失败重试和任务删除。
- 已完善工作台：展示统计、最近素材和最近项目；项目管理支持卡片拖动位置互换，并在放置前显示目标虚线边框。
- 已实现个人中心：头像、显示名称、邮箱只读展示与语言偏好；账户菜单提供个人中心、切换账号和退出登录。
- 登录页不允许保存明文密码到浏览器本地存储。
- Phase 5 的标签、AI 自动命名、标签、描述和 AI 搜索尚未开始，不得混入当前阶段。

Phase 4 收尾时必须核验：真实账户下的图片 / GIF / SVG / GLB / GLTF 预览、下载保存、移动端预览，以及项目删除时素材与存储文件的清理策略。

当前未实现：邮件验证、忘记密码邮件重设，以及外部依赖文件的完整 GLTF 预览。不得在没有配置邮件服务与安全策略的情况下伪造密码找回功能。

---

# 技术规范

## Frontend

必须使用：

- Next.js 16
- React
- TypeScript
- Tailwind CSS
- shadcn/ui

辅助：

- Zod
- React Hook Form
- Motion

---

## Backend

使用：

- Convex

负责：

- 数据模型
- 查询
- 数据修改
- 后端逻辑

## 本地 Convex 环境

- 克隆仓库后的开发者必须运行 `pnpm convex:dev`，创建或绑定自己的 Convex 开发部署。
- 前端通过 `NEXT_PUBLIC_CONVEX_URL` 连接部署；Convex Auth 使用部署自动提供的 `CONVEX_SITE_URL`，不要在前端 `.env.local` 手动覆盖它。
- `.env.local`、`.convex/`、生产部署地址、邮件服务密钥和其他密钥不得提交到 Git。
- 团队测试与生产必须使用独立的共享部署；不要把本地开发数据和生产数据混用。
- 对数据模型或认证函数的变更，必须先在开发部署验证，再进入共享测试环境。

## 认证与账户安全

- 使用 Convex Auth 的 `Password` Provider；密码仅以安全哈希形式保存，禁止读取、记录或存储明文密码。
- 禁止实现“记住密码”或以 localStorage、sessionStorage、Cookie 自行保存密码；登录保持状态仅由认证会话负责。
- 若实现忘记密码，必须采用邮箱一次性验证码或链接验证后重设新密码：短时有效、单次使用、限流、统一响应文案，并在重设后撤销其他会话。
- 头像上传必须限制 JPG、PNG、WEBP 和 5 MB；个人资料只能由当前认证用户更新。

---

## 3D能力

使用：

- Three.js
- React Three Fiber

仅用于：

GLB / GLTF 素材预览。

禁止无意义添加3D效果。

---

# 代码规范

必须：

- TypeScript strict
- 组件拆分
- 类型完整
- 避免重复代码

禁止：

- any
- 超大组件
- 随意修改架构

---

# UI规范

设计参考：

- Linear
- Vercel
- Figma

要求：

- 简洁
- 专业
- 响应式
- 支持暗色模式

必须使用：

shadcn/ui

## 视觉与交互开发规范

项目采用 Dark Holographic Workspace 视觉方向：

- 深色专业背景作为基础
- 玻璃拟态作为容器材质
- 全息渐变作为局部品牌高光
- 产品信息优先于装饰效果

实现要求：

- 使用 Tailwind CSS 和 shadcn/ui 组件实现界面
- 全息效果应通过透明度、背景模糊、渐变描边、内侧高光和柔和阴影组合实现
- 背景模糊必须同时支持 `backdrop-filter` 和 `-webkit-backdrop-filter`
- 全息色彩主要用于 Hero、CTA、选中状态和重点素材，不要应用到所有卡片
- 采用统一的圆角、透明度和阴影层级，避免每个组件使用不同的视觉规则
- 背景光场可以缓慢流动，但不能持续高频闪烁
- Hover 和 Active 状态使用轻微的透明度、光泽、位移或缩放变化
- 所有超过基础 Hover 的动画必须支持 `prefers-reduced-motion`
- 移动端需要降低 `backdrop-blur` 强度，并限制同时出现的玻璃元素数量
- 文字、表单控件和按钮必须满足 WCAG AA 对比度要求
- 必须提供不支持背景模糊或减少透明效果时的实色回退

组件边界：

- 推荐将通用效果拆分为 `GlassPanel`、`PrismaticButton`、`HolographicBackground` 等可复用组件
- 使用 Motion 时从 `motion/react` 引入，并将交互隔离在独立的 Client Component 中
- 页面默认优先使用 Server Component
- 禁止使用 Three.js 制作无意义的背景或装饰效果
- 禁止为每个素材卡片添加无限循环动画
- 禁止使用彩虹渐变承载长段正文或关键数据
- 禁止用视觉光效替代错误、成功和警告等语义状态

---

# 文件处理规范

素材类型：

支持：

- png
- jpg
- webp
- svg
- gif
- glb
- gltf

当前阶段：

使用 Convex File Storage，素材元数据与文件访问均按 Convex Auth 的稳定用户 ID 和项目做权限校验。不得用会在重新登录后变化的会话标识作为资源归属字段。

后续：

将存储能力抽象为适配层后，再评估迁移 Cloudflare R2。项目删除必须明确级联删除素材记录与对应存储文件，不能遗留孤儿素材。

---

# 输出规范

每次完成任务后输出：

1. 修改文件列表

2. 实现内容

3. 测试结果

4. 下一步建议
